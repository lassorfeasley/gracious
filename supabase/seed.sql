-- Dev seed (run after migration, replace UUIDs with real auth.users ids after signup)
-- This file is reference-only; create users via the app signup flow.

-- Example: after creating owner@test.com via signup:
-- UPDATE public.users SET role = 'owner' WHERE email = 'owner@test.com';
